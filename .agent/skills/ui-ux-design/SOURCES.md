> Design Motion is a reference source inside the standalone `ui-ux-design` skill, not its source of truth.

# Sources and Attribution

Primary source:
- https://designmotionhq.com/patterns — current Design Motion UX Pattern Library (73 patterns, seven categories).
- Individual pattern pages are linked inside each pattern reference.

This package paraphrases and restructures the published pattern guidance for use as an AI-agent reference. It does not reproduce the source site's full text or video scripts.

Verification levels:
- **direct-page**: key guidance was checked against the individual published page.
- **catalog-only**: title/tagline is taken from the library index; detailed rules are intentionally not invented.

Video note:
- The library states that its pattern library includes video breakdowns plus written insights. This package only claims video-derived knowledge when the video itself has been separately reviewed.
